# Adaptive-OCGCN dependencies
# Versions below are the ones used for the experiments reported in the paper.
# A PyTorch build with CUDA is recommended for GPU training:
#   conda install pytorch torchvision pytorch-cuda -c pytorch -c nvidia

torch>=2.5.0
torch-geometric>=2.8.0
numpy>=1.26
scipy>=1.13
scikit-learn>=1.5
pandas>=2.2
networkx>=3.3
karateclub>=1.3.3
matplotlib>=3.9
texttable>=1.7
openpyxl>=3.1
tqdm>=4.66
